<h1>レシピ新規投稿</h1>
  <%= form_with model: @recipe do |f| %>
    <h4>写真</h4>
      <%= f.file_field :image, accept: "image/*" %>
    <h4>料理名</h4>
      <%= f.text_field :title %>
    <%= f.submit '投稿' %>
  <% end %>